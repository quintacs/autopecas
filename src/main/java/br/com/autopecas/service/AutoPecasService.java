package br.com.autopecas.service;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
@Transactional
public interface AutoPecasService {

	/*
	 * void save(Object object); void update(Object object); void ubdateById(long
	 * id); void delete(long id); <T> List<T> list(); <T> List<T> list(Class<T>
	 * classe);
	 */
	
}
